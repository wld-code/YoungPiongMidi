# YoungPiongMidi — top-level ESP-IDF project build file.
#
# Target: Espressif ESP-SensairShuttle v1.0 (ESP32-C5-WROOM-1-N16R8).
# Build with:  idf.py set-target esp32c5 && idf.py build

cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(YoungPiongMidi)
